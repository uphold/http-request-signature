{
  "name": "http-request-signature",
  "version": "0.0.2",
  "license": "MIT",
  "main": "src/index.js",
  "scripts": {
    "changelog": "github-changelog-generator --future-release=$npm_package_version --owner=uphold --repo=http-request-signature > CHANGELOG.md",
    "lint": "eslint --cache .",
    "version": "npm run changelog && git add -A CHANGELOG.md src",
    "test": "jest --env=node"
  },
  "dependencies": {
    "debugnyan": "^2.0.1",
    "eslint": "^4.6.1",
    "lodash": "^4.17.4",
    "standard-http-error": "^2.0.0",
    "validator.js": "^2.0.3",
    "validator.js-asserts": "^3.1.0"
  },
  "devDependencies": {
    "@uphold/github-changelog-generator": "0.4.1",
    "eslint-config-uphold": "0.0.1",
    "jest": "^21.0.0",
    "should": "^12.0.0"
  },
  "engines": {
    "node": ">=7.10.0"
  }
}
